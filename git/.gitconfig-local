[user]
	useConfigOnly = true
	# name = YOUR_NAME
	# email = YOUR_EMAIL_ADDRESS
	# signingkey = YOUR_GPG_SIGNING

# If user.signingkey is set enable commit.gpgsign to enable automatic signing of commits
[commit]
	gpgsign = false
